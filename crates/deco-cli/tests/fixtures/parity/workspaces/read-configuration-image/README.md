# Fixture Workspace

Placeholder workspace for the `read-configuration-image` parity scenario.

The future harness will copy or mount this directory when executing the same command
against `deco` and upstream `devcontainer-cli`.
